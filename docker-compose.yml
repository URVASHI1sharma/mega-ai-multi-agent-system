#This is docker file
version: "3.9"

services:

  multi-agent-api:
    build: .
    container_name: mega-ai-system

    ports:
      - "8000:8000"

    command: uvicorn backend.main:app --host 0.0.0.0 --port 8000

    volumes:
      - .:/app